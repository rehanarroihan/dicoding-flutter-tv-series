name: tv
description: TV Show feature module for ditonton application.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  cached_network_image: ^3.4.1
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.2
  mocktail: ^1.0.3
  mockito: ^5.0.8
  build_runner: ^2.0.4
